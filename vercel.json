{
    "version": 2,
    "buildCommand": "npx prisma generate && npx tsc",
    "installCommand": "npm install",
    "functions": {
        "api/index.js": {
            "maxDuration": 30
        }
    }
}